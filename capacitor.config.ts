import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.tikat',
  appName: 'tikat',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
